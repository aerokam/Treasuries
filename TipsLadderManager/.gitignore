# Apps Script — kept in Google Sheets, not in repo
appsscript/

# Auto-memory (Claude Code internal)
memory/


# Legacy data folder — ignored, EXCEPT the sample holdings the app fetches in prod.
# (data/* not data/: a fully-excluded dir can't have a file re-included under it.)
data/*
!data/SampleHoldings.csv
# Wrangler local state
.wrangler/
tips-data-worker/.wrangler/
node_modules/

# Playwright results
test-results/
playwright-report/
