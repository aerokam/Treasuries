# 3.0 TIPS Ladder Rebalancing (Status: Codified / Implementation In-Progress)

## Dependencies

**Requires:** 2.0 TIPS Ladders

**Inherits all terms from 2.0 TIPS Ladders:**

- [DARA](../../knowledge/DATA_DICTIONARY.md#dara), [ARA](../../knowledge/DATA_DICTIONARY.md#ara), [Gap Years](../../knowledge/DATA_DICTIONARY.md#gap-years), Synthetic TIPS construction
- Duration Matching (lower/upper brackets, retained bracket excess, Future 30Y Cover)
- [P+I per TIPS](../../knowledge/DATA_DICTIONARY.md#pi-per-tips), Construction Method (longest to shortest)

**Inherits all formulas from 2.1 TIPS Basics:**

- [indexRatio](../../knowledge/DATA_DICTIONARY.md#index-ratio), [parValue](../../knowledge/DATA_DICTIONARY.md#par-value-adjusted), adjustedAnnualInterest
- lastYearInterest, [piPerBond](../../knowledge/DATA_DICTIONARY.md#pi-per-tips)

---

## Purpose

Rebalance an existing TIPS ladder to align it with its desired-amount targets (DARA), **which may vary year to year** (see 2.0 §Duration Matching — per-year DARA may be uneven). There is a **single rebalance**: it rebuilds the ladder from the current portfolio toward the per-year DARA targets, buying and selling TIPS as required. (An earlier "Gap" mode that did minimal trades was scrapped; the `method` parameter, the `#method` UI input, and the branches that tested it were removed. `src/rebalance-lib.js#runRebalance` takes no `method`.)

## Context

TIPS ladders require periodic rebalancing as new TIPS are issued — filling former gap years so their coverage can move from bracket excess into the year's own rung — and as market prices drift the income profile. A rebalance realigns the ladder to its per-year DARA targets using the current portfolio.

---

## Rebalancing-Specific Concepts

### Ladder Range

- **firstYear**: Earliest maturity year in the holdings.
- **lastYear**: Latest maturity year in the holdings.
- **Gap years**: the maturity years below 2040 for which no 10-year TIPS has been issued, determined by checking the outstanding-TIPS data.
- **DARA for any year in `[firstYear, lastYear]`**:
  - A funded year with no current holdings: DARA is assumed to be 0.
  - A gap year (always empty by definition): DARA is assumed to be the ladder's median DARA, not 0, since its income is funded via bracket excess rather than its own holdings.
- **`lastYearOverride`** (optional UI parameter): When provided, replaces the derived `lastYear`. May be a gap year (e.g., 2039) or an extended year (e.g., 2042). The dropdown defaults to the derived `lastYear`.
  - **Shortening**: When the override is earlier than the derived `lastYear`, all holdings in the years after it are sold in full (produce sell rows in output). These years are excluded from the rebuild.
  - **Extending**: When the override is later than the derived `lastYear`, the added years default to the same DARA as any other empty year (the ladder median), unless the user sets a per-year value for them. The appropriate TIPS for those years are identified from market data and buy rows are generated.
- **`firstYear` UI inference**: When a Format 4/5 CSV is loaded, if `derivedFirstYear` is a pre-gap 10-year TIPS year and all its holdings have `excessQty = qty` (pure bracket, no funded component), the UI infers the true `firstYear` using: `numGapYears = round(totalExcessPI / roughDARA)` → `firstYear = 2040 − numGapYears`. `totalExcessPI = excessQty(2036) × piPerBond(2036) + excessQty(2040) × piPerBond(2040)`. `roughDARA` = median ARA of non-bracket, non-gap funded years. This works because bracket excess P+I ≈ `numGapYears × DARA` (spec §Gap Year Coverage Model). The inferred gap year becomes the default dropdown selection. For Format 1/2/3 CSVs (no `excessQty`), no inference is attempted and `firstYear` defaults to `derivedFirstYear`.
- **`firstYearOverride`** (optional UI parameter): When provided, replaces the derived `firstYear` (earliest year with holdings). Holdings in years `[derivedFirstYear, firstYearOverride)` are sold in full (produce sell rows in output). These years are excluded from the rebuild. **The per-year DARA table follows the same override**: it is keyed off `rebal-first-year` when set, not always off the earliest held year, and any stored per-year value outside the resulting `[firstYear, lastYear]` is dropped from the table — narrowing First Year removes the excluded year's row rather than leaving a stale one behind.
  - **firstYear as a gap year**: When `firstYearOverride ∈ {2037, 2038, 2039}` (gap years), the ladder starts inside the gap years. No holdings exist below the gap, but the active lower bracket (most recently issued 10-year TIPS) is still identified from `tipsMap`. `identifyBrackets` falls back to `tipsMap` when the holdings search yields no lower bracket, setting `lowerCUSIP`, `lowerYear`, and `lowerMaturity` from `tipsMap`. Standard 2-bracket duration-matching weights apply. In full rebalance, a BUY order for 2036 excess is generated (`qtyBefore = 0`, `excessQtyAfter = ROUND(totalBlockCost × lowerWeight / costPerBond_2036)`). Build: the 2036 bond is inserted into `yearBondMap`/`rangeYears` before the sweep with `fundedYearQty = 0` (pure bracket excess). The bracket identification step excludes holdings from years < `firstYear` (being sold) so they don't incorrectly satisfy the lower-bracket search.
  - **Retained excess with firstYear inside the gap**: When retention is on and `firstYear ∈ {2037, 2038, 2039}`, `minGapYear = firstYear` and `minGapYear − 1` is itself a gap year with no 10-year TIPS. The active lower bracket search must walk `tipsMap` for the longest-dated 10-year maturity strictly below `minGapYear` (same logic as `anchorBefore`) rather than checking year `minGapYear − 1` exactly. That maturity is also the `brackets.lowerYear` returned by `identifyBrackets`. With no retained maturity distinct from it, the lower brackets reduce to the plain 2-bracket case — correct behavior, no error.

### lastYear as a Gap Year

When `lastYearOverride` is a gap year (i.e., `lastYear ∈ gapYears`), the upper bracket year (2040) is **not a funded rung** — all its holdings are treated as bracket excess:

- `fundedYearQty(2040) = 0` in Phase 3 and Phase 4 (`yearDara(2040) = 0`)
- `excessQtyBefore(2040)` follows the standard cost formula (`ROUND(totalBlockCost × weight_2040 / costPerBond_2040)`). Since `fundedYearQtyAfter(2040) = 0`, `fundedYearQtyBefore = qtyBefore − excessQtyBefore` approaches zero when the portfolio was correctly built.
- Phase 2 (`calculateGapParameters`): 2040's LMI contribution uses **actual current holdings qty** (not a target computed from DARA) since no funded-year target exists for 2040.
- `rungCount` = `lastYear − firstYear + 1` (gap years count as rungs; their income is funded by bracket excess)

**UI default for `lastYear`**: The dropdown defaults to the latest maturity year in the holdings (Ladder Range above) — e.g. a held 2040 TIPS defaults `lastYear` to 2040, funding it as a rung. To end the ladder at a gap year instead (treating a held 2040 TIPS as pure bracket excess), the user selects 2039 from the dropdown, which includes all available TIPS years to allow extension.

### Ref CPI Date

**There is no Ref CPI date control, in either mode.** Both Build and Rebalance look Ref CPI up for
the settlement date. The info strip shows Trade and Settle, and nothing else: the settlement date
already names the date every figure is stated at.

A Rebalance is a set of trades that will settle on the settlement date, so its costs and amounts are
only meaningful at that date. A Build prices at today's market, and stating those prices at an
older Ref CPI would combine index ratios from one date with prices from another, producing a ladder
that existed on no day: the two move independently, so the result is not the ladder of either date.

The one thing an earlier date might legitimately have been wanted for — holding a saved ladder's real
target steady over time — is what §DARA Reference Date does instead, from the date recorded **in the
file**. A holder who built a year ago has the holdings; loading their CUSIP/Qty file into Rebalance
prices what they actually own at today's market, which is both more accurate and the purpose of
Rebalance. Test ladders genuinely stated at an older date are built from that date's own market data
(`tests/fixtures/yearago/`), not by back-dating one control against current prices.

*(A Build-only override existed until 2026-08-27, added to simulate a ladder built earlier so the
saved-and-reloaded path could be exercised end to end. The year-ago fixtures do that properly, on one
date's data throughout, so the control was removed rather than left as a way to produce figures that
mix two dates.)*

**Ref CPI interpolation popup** (level-3 drill from any Ref CPI row): shows the settlement date as
"Settlement Date" and the m-3 and m-2 CPI-U values that correspond to it.

### DARA Reference Date

DARA is a **real** amount, denominated at a Ref CPI date. A rung's ARA is
`qty × 1,000 × (RefCPI_active ÷ baseCPI) × (1 + coupon ÷ 2 × nPay)`, so both sides of the comparison
`ARA vs DARA` move with the active Ref CPI. That makes DARA and its Ref CPI date a **pair**: 40,000
at one date and 40,000 at another are different real amounts, differing by the inflation between
them.

Within a single session this is invisible, because one Ref CPI date governs everything. It becomes
visible the moment a ladder is saved and reloaded later: the exported `#fundedYear,dara` block is a
list of bare numbers, and reading them at a later date silently re-denominates every one of them.

**The export records the basis.** `#params` carries `refCpiDate=<YYYY-MM-DD>` — whichever Ref CPI
date was in effect when the file was written (the settlement date normally, or the user's override
when one is set, §Ref CPI Date). Written by both the CUSIP/Qty export and the standalone
DARA-plan file.

**The import scales.** When the recorded date differs from the Ref CPI date in effect, every DARA
value is multiplied by `RefCPI(active) ÷ RefCPI(recorded)`. A target of 40,000 stated at an earlier
Ref CPI date becomes the equivalent figure at the current one, so the ladder keeps the same real
purchasing power rather than being read as 40,000 of today's smaller dollars. In Rebalance the date
in effect is always the settlement date (§Ref CPI Date), so the file's own date is the only
thing the scaling reads, and holding a target steady over time stops being something the user has to
set up. In Build it is whatever date Build is simulating.

**Why this replaces back-dating the Ref CPI date.** Setting the Ref CPI date back to a ladder's build
date holds its real target steady, and is self-consistent for the gap and bracket math, since the
synthetic rungs are modeled as issued on that date too (2.0 §Synthetic TIPS for Gap Years) and every
real bond's index ratio moves with it. What it breaks is the handoff at issuance: a gap-year TIPS
issued later carries a dated date Ref CPI from its own dated date, so against a back-dated Ref CPI its index
ratio is below 1 and it delivers less than the synthetic it replaces, forcing a purchase that has
nothing to do with the ladder's real target. Restating DARA leaves the basis at today, where the
synthetic and the newly issued bond agree.

**What is invariant.** With DARA restated, inflation alone produces no trades. Funded-rung quantity
is `(DARA − LMI) ÷ P+I`, and all three terms scale with the price level. Bracket excess quantity is
`gap cost × weight ÷ cost per bond`, and both cost terms scale with it. Only the synthetic TIPS
quantity moves — correctly, since a synthetic issued today covers less real value than one modeled
at the earlier date would have — and the bracket bonds covering it have accreted by the same factor,
so the holdings requirement comes out flat.

**Visibility.** A scaled import says so in the status strip: the file's Ref CPI date, the date scaled
to, and the factor. The numbers on screen differ from the numbers in the file, so the user is told
why.

**There is no way to decline the scaling.** Using a file's values as written means reading a target
stated at an earlier Ref CPI date as though it were stated at the settlement date, which quietly cuts
the real target by the inflation in between — the exact failure this section exists to prevent. A
control that reproduced it on demand would only ever produce a wrong answer, so an earlier revert
control was removed. A user who wants a different target edits DARA, which is a different act from
misreading the basis of the one in the file.

**Files with no recorded Ref CPI date.** Every export written before this, and any hand-written DARA
plan, carries a `#fundedYear,dara` block with no date. The app cannot know the date and does not
guess: the values are used as written — exactly the earlier behavior, so nothing regresses — and the
status strip offers to set the file's Ref CPI date, opening a date picker. Supplying a date scales
the values from it; ignoring it leaves them alone. The offer stays available afterward, so a date
entered wrongly can be replaced; the picker reopens on the date last supplied. It states a fact about
the file rather than choosing what the app prices at.

**This entry restates per-year amounts and nothing else.** Rebalance takes two dates, and they are
separate entries with separate offers, sharing only the picker frame they open in:

- **The file's Ref CPI date**, this one. Offered only where the loaded file carries per-year amounts
  but records no Ref CPI date, because amounts are the only thing it acts on.
- **Cash received since** (2.0 §Available Cash), setting which received payments Available Cash includes.
  Offered wherever the app filled that figure in from a file recording no date, which includes a
  position file carrying no amounts at all. It is not a Ref CPI date and restates nothing, and it is
  not a free date either: the choices are the coupon payment dates this portfolio was paid on, since
  cash arrives on no other day.

They were a single entry until 2026-08-28, which read as though supplying a date to a position file
would restate amounts that file never carried, and did nothing visible when it was tried. A file that
records its own `refCpiDate` still drives both automatically, unchanged: it is the settlement Ref CPI
date in effect when the file was saved, so it is both the basis of its amounts and the point after
which its cash flows have been received.

**Other file formats are unaffected.** Broker files, the tipsladder.com format, and plain `cusip,qty`
files carry no DARA at all — the per-year values are derived from the holdings at the Ref CPI date in
effect (§Per-Year DARA from Portfolio), so they are already in the current basis by construction.
There is nothing for a Ref CPI date to restate on such a file, and the entry is not offered there. The
corollary is worth stating because it is not obvious from the panel, which shows per-year amounts for
these files like any other: those amounts are the app's reading of the holdings at today's basis, not
the file's own figures, so restating them to an earlier date would misstate what the holdings are
currently worth. Scaling per-year amounts to a Ref CPI date requires a file that carries the amounts,
which is a DARA plan or one of our own exports.

### DARA

#### Per-Year DARA from Portfolio (File Load) — mirror current holdings, **no inference**

When a holdings file **without an explicit `#fundedYear,dara` block** (broker, legacy Format-5, tipsladder Format-4) is loaded in Rebalance mode, the per-year DARA panel is populated to **mirror the current portfolio** — the Annual Real Amount (ARA) each rung already throws off. **No DARA is inferred, scaled, or self-finance-solved at load.** (Our own exports carry the `#fundedYear,dara` block and take the exact-reproduction path instead — see 2.1 Broker Import.)

1. **Raw ARA, every in-range year**: process holdings longest-to-shortest, accumulating LMI (annual coupon from later bonds). For a **held** year `ARA_year = Σ(qty × piPerBond) + LMI_from_later`; for an ordinary **empty** in-range year (TIPS exist for that maturity, the user simply holds none) `ARA_year = LMI_from_later` alone. Every year in `[firstYear, lastYear]` is written to the per-year store — empty years included — so the panel shows each rung's true current income. An empty year between two holdings shows just the coupon dripping down from the later bond (e.g. ~$230), **not** a full rung.
2. **Gap years use the funded-year median**: a structural **gap year** (2037–2039 — no TIPS issued for anyone, identified by `getGapYears(tipsMap)`) cannot be held directly, so there is no holding to read a DARA from. The bare LMI dripping down would understate the rung (it is funded by bracket excess at the adjacent years, not by a bond of its own). Each gap year with no supplied DARA therefore falls back to the **funded-year median** (step 3). *(Future-30Y years 2057+ are out of scope for basic files — they only appear via our own export, which carries an explicit `#fundedYear,dara` block.)*
3. **Bracket-candidate excess flag (held years only)**: the median is taken over the **held** funded years; empty LMI-only years are excluded so they cannot collapse the median. A gap-adjacent bracket-candidate year whose raw ARA exceeds that median is no longer silently capped to it (the old 1.5×-median cap is removed). Instead the year is **flagged** and its per-year store entry is left unset — see §Before-State Preview and Bracket-Year Excess Detection below for the full mechanism (which candidate years are checked, how a single lower-bracket candidate is chosen, and what happens once the user supplies its DARA). This is a **preview-time** flag only; it does not change how any other year in this mirror is populated.
4. **Scalar DARA field**: set to the held-year median (rounded) — the representative rung size, used as the fallback only when the user later clears the per-year store and types a single scalar.
5. **Panel auto-opens** so the user can review or modify individual years before running.

This per-year store is the **shape** the rebalance preserves. At **load** nothing is scaled or solved — the panel shows the portfolio as-is, **except** the flagged bracket-candidate year(s) from step 3, whose store entry is deliberately left unset pending user input (§Before-State Preview and Bracket-Year Excess Detection) — every other year mirrors unconditionally. The self-financing adjustment happens at **Run** (next subsection). The user may also **explicitly** reshape before running: type a new scalar in the DARA field, edit individual years, or select a range of rungs directly on the table and use Set DARA / Infer DARA (see §Per-Rung DARA Selection). Nothing else is inferred on the user's behalf at load.

#### Before-State Preview and Bracket-Year Excess Detection

**The Before state renders immediately on load, not gated behind Run.** As soon as holdings are loaded / account(s) selected in Rebalance mode, the table renders Qty Before / Cost Before / Amount Before for every ordinary funded year, computed directly from current holdings + market data (price, index ratio) — no DARA, no duration matching. This is a **standalone computation**, architecturally decoupled from `runRebalance`/`runFundedRebalance` (§Funding the rebalance below): it does not call, and must not import, the duration-match/self-financing engine. This is not a second copy of that math (single-source-of-truth, top-level CLAUDE.md) — it is a genuinely different, simpler computation (holdings valuation) with no engine involved, not a duplicate of the sizing algorithm. After-side columns (Qty/Cost/Amount After, Qty Δ, Cash Δ — 5.0 §Rebalance-only columns) and the After-dependent controls (the Export menu's Ladder CSV / CUSIP-Qty options, Trade Ticket, Duration links, Avg Yld/Avg Mod Dur — 6.0 §Form Card Layout) stay blank/hidden/disabled exactly as before, until the user clicks **Rebalance Ladder**. Run itself is unchanged: it still runs the full duration-match/self-financing engine exactly as today and fills in the After side. **Expand/Collapse All** (6.0 §Collapsible Groups) is NOT in that hidden set — the ladder table with its collapsible funded-year groups is already fully visible pre-Run, so those two buttons are available as soon as this before-state table renders.

**Bracket-year excess detection** runs at the moment of this preview, before any Run, over candidate years that are structurally fixed (not duration-matched):
- **Lower bracket candidates**: 2032–2035 (`LOWEST_LOWER_BRACKET_YEAR` through the year below `minGapYear` — DATA_DICTIONARY §5.1, §Bracket Identification Rules above).
- **Active Lower Bracket candidate**: the [Active Lower Bracket](../../knowledge/DATA_DICTIONARY.md#active-lower-bracket) year itself (2036 today).
- **Upper bracket candidate**: 2040.
- **Future 30Y cover candidates**: 2052 and 2056 (2.0 §Future 30Y Cover Pair).

For each candidate year that is currently **held**, its raw ARA (step 1 above — held qty's P+I plus incoming LMI) is compared to the **held-year median** (step 3 above — held funded years only, excluding the candidate itself):
- Raw ARA ≤ median: **no flag** — the year renders as an ordinary funded year, no special treatment, no Gap sub-row.
- Raw ARA > median, **lower bracket (2032–2035 — every Jan-maturity TIPS below the Active Lower Bracket year; the active bracket itself is evaluated separately, below, not part of this pool)**: if exactly one candidate exceeds the median, that year is flagged. If more than one exceeds, only the one with the **highest excess** (`rawARA − median`) is flagged — the same "Excess ARA Priority" rule §Bracket Identification Rules §Retained Maturities already uses for the real engine's retained-maturity identification (highest `araByYear[y] − DARA` wins, not latest-maturing) — and the others render as ordinary funded years. (An earlier revision of this section said "latest-maturing wins"; that picked the wrong year on real holdings whenever the largest retained excess sits at an earlier-maturing candidate than a smaller-excess later one — fixed to match the engine's own established rule instead of inventing a different one for the preview.)
- Raw ARA > median, **Active Lower Bracket (2036)**, **upper bracket (2040)**, and **Future 30Y covers (2052, 2056)**: each is evaluated independently against the same held-year median (if held) — none of these compete against each other or against the lower-bracket pool above for a single slot; any subset may flag simultaneously. The active bracket is deliberately kept out of the lower-bracket pool's "highest excess wins" competition — it's expected to carry both real funded income and its own gap-coverage excess, so it must be able to flag alongside a genuine older retained-bracket flag (e.g. 2034) rather than always winning that pool's tie-break by virtue of usually holding the largest raw ARA.

**When a year is flagged**, the app does not silently cap it and hide the excess (replacing the old step-3 cap above) — but it also doesn't leave the year blank pending user input. Instead it shows an immediate **best guess**: the flagged year's DARA input (5.0 §Funded Year Group Header Row §DARA Input) is pre-filled with the held-year median (the same median used for detection), and the excess — `raw ARA − median` — is shown right away in that year's Gap sub-row, exactly as if it had been entered normally. The pre-filled value is marked with the existing amber ⚠ "auto-set to median" indicator, signalling it's a guess pending review, not a confirmed figure. `index.html` collects the flagged years in `_bracketAutoCapYears` and passes them to `src/render.js#renderTable`, which renders the indicator and its tooltip in `src/render.js#daraInputHTML`. The tooltip opens "Auto-set to median." and then states which of the two cases applies: a maturity year whose ARA exceeds the median ARA of the maturity years that hold TIPS, or a gap year or Future 30Y rung for which no TIPS have been issued and so no quantity exists to infer a DARA from.
- A flagged lower-bracket, Active Lower Bracket, or 2040 candidate's excess is presented as covering the gap years it apparently funds (2037–2039, 2.0 §Gap Years) — the gap years' own DARA inputs are also pre-filled/flagged the same way where they have no other source.
- A flagged 2052/2056 candidate's excess is presented as covering the Future 30Y years it apparently funds (the Future 30Y years in `[firstYear, lastYear]`, 2.0 §Future 30Y Rungs), same treatment.

If the user edits a flagged year's DARA input to a different value, the ⚠ clears for that year and the excess recalculates immediately — `raw ARA − entered DARA` — updating the Gap sub-row live. This is plain subtraction, never duration-matching, for both the guessed and the user-confirmed figure. Loading a standalone DARA-plan file (2.1 §Standalone DARA-plan file) clears every ⚠ the same way, for every year: the loaded plan is an explicit value for each year it covers, the same kind of override as a hand-typed one, just applied to the whole ladder at once instead of one rung at a time.

**Qty Before / Cost Before split for a flagged year.** For every OTHER (unflagged) year these never depend on the funded/excess split — the full held quantity is simply the funded-side figure, as before. But for a flagged year, showing the full held qty/cost against a much smaller (guessed or entered) Amount reads as broken (a funded row with a large qty/cost next to a tiny Amount, and an excess sub-row with a dollar figure but no qty/cost backing it). Once the year's DARA is known (guessed or entered), split qty/cost too, using the same LMI-based conversion this app already uses elsewhere for DARA→qty (§Intentional empty rungs above: `round((DARA − LMI) / piPerBond)`) — still not duration-matching, just direct bond arithmetic:
- `fundedYearQtyBefore = round((DARA − LMI_before) / piPerBond)` (`LMI_before` is the year's own incoming later-maturity interest, already computed for the raw-ARA figure above; `piPerBond` is the flagged CUSIP's own P+I per bond)
- `fundedYearCostBefore = fundedYearQtyBefore × costPerBond`
- `excessQtyBefore = heldQty − fundedYearQtyBefore` (floored at 0 — a DARA entered above the year's own raw ARA is the user's prerogative, not an error, but excess never goes negative)
- `excessCostBefore = excessQtyBefore × costPerBond`

This makes the flagged year's funded row and Gap sub-row show internally consistent qty/cost/amount, matching how the real engine already presents a resolved funded/excess split after Run — the pre-Run guess should look like a preview of that, not a different shape.

**Scope: preview only.** This flagging/prompting logic is purely a pre-Run display aid. It has no effect on the real duration-match/self-financing engine at Run time — Rebalance Ladder still reads whatever per-year DARA is on the form (the same `daraByYear` map the engine already consumes today, §Per-Year DARA (`daraByYear`) below), completely independent of how the preview displayed things before Run.

#### Funding the rebalance — self-financing scale with pinned rows (Run)

A rebalance must be **self-financing**: net cash (`costDeltaSum`) is a small **non-negative** number — the proceeds of what is sold cover what is bought, with no external cash added, and no surplus cash is left unspent either. The search is direction-agnostic: it finds the single DARA level at which the *whole* rebalance — the bracket trade together with every funded rung — nets to that small non-negative figure, by scaling the funded rungs' per-year targets by a single factor so the ladder keeps its (possibly uneven) shape.
- When the duration-match bracket excess (sized from the gap years' total cost, see 2.0 §Excess Quantities) is a net **purchase**, the cash to buy it is raised by **selling across the funded rungs** — the scale factor pulls their targets down.
- When [Retained Bracket Excess](../../knowledge/DATA_DICTIONARY.md#retained-bracket-excess) exceeds its duration-matched target and is sold down (2.0 §Retained Bracket Excess), the bracket trade nets a **sale** instead, freeing cash rather than requiring it — the same search instead pulls the funded rungs' targets **up**, spending the proceeds by buying slightly more everywhere, so no surplus is left sitting as unspent net cash. Either way it is the one coupled solve below, never two separate mechanisms.

**Pinned rows are exempt from the scale.** A row the user has hand-typed into its own per-year input, or set through a selection **Set DARA** (§Per-Rung DARA Selection), is a **stated target**: it keeps that exact value, and the scale sweeps only the rows the user left alone. The pinned row's own trade still counts toward the self-financing total — it just is not itself moved to reach it. (A selection **Infer DARA** does *not* pin: it already self-finances the years it touches, and those years scale with the rest on the next Run.) Rows the user has not touched (`pinnedDaraByYear` empty) means the whole shape scales together — the original behaviour.

This is **one coupled solve, not a conditional step**, because the inputs are interdependent: the gap-year DARA is the funded-year median, the gap years' total cost depends on that median, the excess to buy depends on the cost, and the cash raised (hence the scale factor) depends on the excess. A **binary search on a single DARA level** dissolves the loop: at each trial level `L` the swept rows are rebuilt at `k = L / median` (each swept rung × `k`), pinned rows hold their stated value, the rebalance is run, and `L` is driven to the **highest value at which `costDeltaSum ≥ 0`**. The `median` the factor pivots on, and the level returned as the scalar fallback, are taken over the **swept rows only** — for a stated plan every year carries its own value so this fallback is never consulted for a real rung.

- **Implementation:** `inferScaledDARAFromPortfolio` with `flat = false` (shape-preserving) and `pinnedDaraByYear` carrying the stated rows. This is the **single source** for the solve — the same function the selection-toolbar **Infer DARA** action calls (§Per-Rung DARA Selection; it uses `flat = true`, a flat shape, and pins the same way; Run uses `flat = false`, the natural shape). Wired into the standard Run path; the displayed panel is **not** rewritten (the user keeps seeing the mirrored/edited shape).
- **The scale is applied ONLY when there is a gap year or Future 30Y year in range.** The scale exists solely to settle the cash the duration-match excess trade at those years produces — buy or sell — against the funded rungs. Run first executes the rebalance on the per-year store directly; the scaled re-run happens only if the engine reports gap years or Future-30Y years in range (`summary.gapYears` / `summary.future30yYears` non-empty) **and** either the plan is still exactly as loaded (`daraPlanUnedited`) or the user has pinned one or more rows of a **stated** plan (`daraPlanIsStated`). With no such block there is nothing to buy, so the direct run already nets to ≈ 0 and **no scaling is done**. If the user has pinned **every** funded rung there is nothing left to sweep — the stated shape runs as entered and its net cash carries whatever surplus or shortfall it implies. This whole direct-run → conditional-scale orchestration is the library export **`runFundedRebalance`** (single home, unit-tested); the UI shell passes the per-year store, the `daraPlanUnedited` / `daraPlanIsStated` flags, the pinned-row map, and the form values.
  - **A hand-edited broker/legacy MIRROR (no stated `#fundedYear,dara` block) is not swept row-by-row** — an edit anywhere still switches the scale fully off, as before. The scaled solve for a mirror recovers its shape from the **held** funded years (`computePortfolioARAByYear` + `derivePerYearDara`); ordinary **empty interior years** (a maturity the user simply holds none of — *not* a structural gap year) are absent from that map and would fall back to the **scalar** DARA inside `runRebalance`, which sizes them as full rungs and emits spurious BUYs — the search then reads those phantom buys as "excess to fund" and sells the whole ladder down. A **stated plan** carries every year explicitly (including the empty ones, at their incoming-LMI stub), so it has no such hole and can be swept safely; the mirror path stays all-or-nothing until that recovery preserves empty years.

#### Intentional empty rungs and the rung-validation waiver

An **empty interior funded year is an intentional hole by default** — but it is a *default*, not a hard floor. The rule is per-year and provenance-free: the year's **own per-year DARA** decides.

- **Untouched (mirror) empty year** — its DARA is its incoming-LMI stub, so `round((DARA − LMI) / piPerBond)` lands at **0**: a hole, no trade. This is the no-op.
- **Explicitly raised empty year** — when the user lifts that year's DARA above its incoming LMI by at least one bond's worth (a manual edit, or a selection Set DARA — §Per-Rung DARA Selection), the year sizes to **≥ 1 bond and fills normally**. The panel value is the user's stated intent, is pinned against the self-financing scale, and is honored — there is no separate "is this a hole" flag.

Because of the first case, `sizeLadder`'s "every funded year must fund at least one bond" validation (which throws `daraTooLowYear`) is **waived for the rebalance's unheld in-range years** (`optionalYears`): an empty year whose target rounds to 0 is a hole, not a too-low-DARA error. The waiver only matters at the knife-edge (an empty year's mirror DARA ≈ its own incoming LMI, which can round either side of the prelim-LMI estimate); a year the user genuinely funds has target ≥ 1 and never reaches the throw. Build passes no `optionalYears` — every build year is a required rung, so its too-low-DARA error stands.
- **Scope — pinned rows, not the whole plan.** The self-financing scale runs whenever a gap year or Future 30Y year needs funding. A **stated plan** (carries its own `#fundedYear,dara` block — our CUSIP/Qty and DARA-plan exports, `daraPlanIsStated`) is swept row by row: the rows the user has hand-stated (a per-rung edit, or a selection Set DARA) hold their value, and only the untouched rows scale to reach self-financing. Pin every rung and nothing scales — the plan runs exactly as entered. This lets a user restate one rung (e.g. lower a near-year DARA so the rebalance stops buying it) without the tool re-levelling the rest of the ladder away from the file.

  Where a file came from does **not** exempt it from the scale. A CUSIP/Qty file describes a ladder holding no cash, so nothing about the file alone can switch self-financing off. The build→export→import round-trip stays zero-trade because such a plan already funds itself and the search leaves it where it is, not because it is skipped. The exemption previously written here meant our own exports — the only files that always carry a DARA block — were the only files that could never be funded, so an **aged** export (DARA values restated upward to a newer Ref CPI date on import, §DARA Reference Date) reloaded as a ladder that could not pay for itself.
- **A hand-edited broker/legacy mirror is still all-or-nothing.** A plan auto-derived from holdings (a broker or legacy file, which states nothing) is recovered from the portfolio's own ARA by `computePortfolioARAByYear` + `derivePerYearDara`, with a cover-income correction the recovery needs (it under-reports the cover years). That recovery drops the empty interior years, so it cannot be safely swept around pinned rows — any edit to a mirror switches the scale fully off, as before. Only a stated plan, which carries every year explicitly, gets the row-by-row treatment.
- **Which shape gets scaled.** A stated plan **is** the shape: swept directly, taken at face value, with no cover-income correction (a stated plan already carries each year's true DARA and correcting it would count that income twice). A mirror is re-derived and corrected as above. Re-deriving a stated plan would discard the user's stated per-year targets and replace them with a mirror.
- **The search must trial the ladder the caller will execute.** Maturity preference, within-year allocation policy and per-year overrides (§Target CUSIP resolution) decide which CUSIP each year buys and therefore what each trade costs. A search run under the defaults solves for a ladder that is never built: the level it returns can satisfy `costDeltaSum ≥ 0` under `equal`/`last` and still land well short once executed under the user's actual settings. Every trial therefore carries the caller's own `maturityPref`, `allocationPolicy`, `yearRankOverrides` and `yearOverrides`.
- **Relationship to 2.0:** this rule sits entirely above 2.0. 2.0 §Algorithm *sizes* each rung from its DARA and credits bracket-excess coupon down as LMI; it says nothing about cash balancing. The self-financing scale chooses the one DARA level at which 2.0's sizing nets to non-negative cash. No 2.0 claim is altered.

#### Scalar DARA Field

The main DARA field is the fallback for funded years that have no per-year entry. If the user types a new scalar value, it applies to all years not overridden in the per-year panel. If the field is blank, the UI uses `_daraByYearPanelDefault` (set at file load from the portfolio median).

**The field displays blank (with a "by year" placeholder hint) whenever a single number would be
misleading** — any variable per-year shape. It shows the real number only for a genuinely flat
ladder. This keeps the top box and the per-year table from disagreeing with each other —
`_daraByYearPanelDefault` (the internal fallback the box's number comes from) tracks the live
per-year store's own median whenever the box is blank, rather than a stale figure left over from
before a selection Infer/Set DARA or a First/Last Year change. Typing a new number into the box
while it's blank starts from nothing, not a resurrected old flat value the user didn't ask to bring
back; an invalid entry (e.g. below $1,000) is left on screen exactly as typed until Run rejects it,
rather than being silently blanked out from under the user before they see why.

#### Re-run Behaviour

On every Rebalance Ladder click, `_daraByYearPanelDefault` is synced to `finalDara` before `getDaraByYear()` is called, ensuring stored per-year values are applied correctly regardless of whether the panel is visible.

### Pre-Ladder Interest (PLI) in Rebalance

When `preLadderInterest = true`, the pre-ladder pool, its zeroing, and its partial-credit
distribution are computed by the **shared `sizeLadder`** (ladder-core.js) — the *same* function build
calls — not by a rebalance-local copy. Rebalance derives the canonical bond set with
`selectLadderBonds(firstYear, lastYear)`, runs `sizeLadder`, and consumes its
`zeroedFundedYears`, `partialCreditYear`, `partialCredit`, `pliCreditByGapYear`, and
`pliCreditByFundedYear`. The pre-ladder pool is sized from a canonical (DARA-based) prelim sweep over
`[firstYear, lastYear]`; `preLadderPool = preLadderYears × totalAnnualInt + preLadderAmdPool`,
distributed short→long with gap years consuming credit before longer funded years.

**Why this matters (history)**: rebalance previously hand-wrote its own prelim sweep + pool, which
drifted from build's at the partial-credit boundary year (2040) — build credited ~$11k there,
rebalance ~$4.3k, yielding a 4-bond / ~$6k discrepancy on a build→rebalance round-trip. Sourcing the
PLI distribution from `sizeLadder` makes the round-trip produce **zero trades / zero net cash**.

### Build ≡ Rebalance "Before" and "After" (unification invariant)

The rebalance **"Amount After"** for every funded year must equal **build's "Amount"** for that year,
and — for a no-trade round-trip of *our own* file — the **"Amount Before"** must equal it too (the held
ladder IS the target). **Every** build funded year must render in the rebalance table. This is the
contract that makes a build → export → import → rebalance round-trip exact, and it is locked by
tests/run.js ("rebal After == build amount per year" **and** "rebal Before == build amount per year",
worst $0 across flat / +PLI / variable / variable+PLI). Three mechanisms enforce it:

**Scope: single-TIPS-per-year builds (`maturityPref='last'`).** The zero-trade round-trip is
guaranteed for the default `maturityPref='last'` build. Rebalance restores an imported file's
`maturityPref` from the `#params` line, but its **target** side still assumes one target CUSIP per
funded year (`bst.targetCUSIP`); it does not yet reconstruct a multi-TIPS target for a
`semiannual`/`all` build (see 2.0 §Maturity Selection Within a Funded Year, 4.0 §ladder-core.js).
`couponPref` is **never** restored on the rebalance side at all — it is Build-only, so a build with
`couponPref='lower'` combined with a non-`last` `maturityPref` round-trips through rebalance using
the `'higher'` default, which can select different same-month TIPS than the build actually holds.
See §Target CUSIP resolution (Within-Year Allocation Policy) below for how the *single* target CUSIP
is chosen from the (possibly multi-TIPS) candidate set each run.

1. **One shared Amount rule — `ladder-core.fundedYearAmount`.** The per-year Amount (and its
   pre-ladder credit) is computed by a *single* exported function that `build-lib.js` **and both** the
   rebalance "Before" and "After" sweeps call — there is no second copy of the formula to drift. It returns
   `amount = principal + ownCoupon + laterMatInt + ownExcessCoupon + AMD + rollCoupon + credit` (where
   `rollCoupon` is the Future-30Y cover-roll coupon credited to 2053–56; see 2.0 §Cover-roll coupon), where for a fully
   PLI-funded (zeroed) year `credit = max(0, DARA_y − fixedIncome)` (reconciled so the row lands exactly
   on its DARA) and otherwise `credit = partialCredit` (the year's non-zeroed pre-ladder share, else 0).
   This matters because `sizeLadder` sizes the PLI pool against the *preliminary* later-mat interest
   (the approximation that decides *which* years zero), so `pliCreditByFundedYear[y]` is a prelim-basis
   credit, whereas a row's ARA is built from the *corrected* components (`lAfter` + own-year excess
   coupon `exIntA` + AMD); the shared rule reconciles the zeroed-year credit to those corrected
   components. Without it a zeroed year overshot its DARA by `(lAfter − prelimLMI)` (e.g. 2034 showing
   $22,957 against a $20,000 DARA under variable DARA + PLI). A *non-zeroed* year that later-maturity
   interest alone over-covers legitimately exceeds its DARA (e.g. 2035 → $31,790 vs a $30,000 DARA) —
   **build shows the same value**; that is not an overshoot, the rung simply needs no bonds.
   Display-only: trades use the sized quantities.

2. **Render fully-covered rungs.** Rebalance emits "After" rows from holdings + buys, so a ladder rung
   that is fully covered (zeroed by PLI, or over-covered by LMI) — hence has no holding *and* no buy —
   would silently vanish (the "skipped 2035" bug). After the holdings/buy rows, a final pass over the
   canonical `rangeYears` emits any missing rung as an informational row (`qtyBefore = qtyAfter = 0`,
   Amount After = `postARAByYear[y]`), mirroring build, which always renders every ladder year.

   The same vanishing failure mode recurs at CUSIP granularity: a synthetic-row pass emits a buy into
   `targetCUSIP` when that CUSIP has no current holdings (e.g. Multi-bracket `newLowerYear`, or an ordinary
   `'all'`/`'saYield'` year whose top-ranked candidate is a brand-new CUSIP never held before). This is
   gated on **the target CUSIP specifically**, not on "this funded year has zero holdings of any kind"
   — a year can already have other held CUSIPs (already emitted via the main holdings loop) while the
   target itself is still a fresh buy that loop never sees. Gating on "any holdings" let that buy
   vanish from the table/Trade Ticket/export while still being counted in the year's Amount After total
   — Amount After would grow with no visible trade to explain it.

3. **"Before" runs the same sweep with held quantities.** "Amount Before" is the current-holdings
   valuation, so it uses *held* qtys — but through the **same** `fundedYearAmount` rule as After, with
   the same component set: own principal + own coupon + LMI + **held excess coupon** (`exIntB`) + AMD +
   credit. Previously `beforeARAByYear` was a separate, cruder sum (`pB + cB + lBefore + amdBefore`)
   that omitted the bracket excess coupon and the pre-ladder credit and over-scaled AMD — so Before
   drifted from build even with zero trades (20k+ deficits at zeroed/bracket years like 2036/2040, and
   ~$500/yr from the AMD basis). The pre-ladder **credit in Before is applied only for our own files**
   (those carrying an explicit per-year DARA block); broker files (inferred DARA) keep their existing
   Before semantics until broker handling is revisited. For a broker file *with* trades, Before ≠ After
   legitimately — that difference is the point of "Before". **Bracket display:** a designated gap
   bracket renders with `*` + a qty-0 excess sub-row even at 0 excess, in **both** build and rebalance
   (`render.js isBracket` uses `excess > 0 || isGapBracket` for both modes) — previously rebalance hid a
   0-excess bracket (e.g. 2036/2040 under high later-year DARA), while build showed it.

**AMD-inclusive DARA inference**: when `dara = null`, the inferred DARA is the median of each
funded year's `P+I + LMI + held-2052 AMD + Future-30Y roll coupon`. Because Option C credits held-to-maturity
2052 AMD (and, for 2053–56, the cover-roll coupon) to every funded year's DARA, the raw `P+I + LMI` median
understates DARA; adding the AMD **and** roll coupon the portfolio actually earns (both scaled by held 2052
excess) recovers the true DARA. The natural-ARA recovery (`inferScaledDARAFromPortfolio`) likewise adds back
`ownExcessCoupon + AMD + rollCoupon` at the cover/post-cover years so the recovered map matches build's identity.

### Per-Year DARA (`daraByYear`)

An optional `Map<year, number>` allowing any ladder year — funded, gap, or Future 30Y — to target a different annual real amount. When provided:

- Each funded year in Phase 4 uses `daraByYear.get(year) ?? DARA` as its target — years absent from the map fall back to the scalar DARA
- Gap years use `daraByYear.get(year) ?? DARA` in the synthetic-qty formula and in the PLI-pool need estimate; a higher gap-year DARA increases synthetic TIPS quantities, which raises `gapParams.totalCost` and therefore increases bracket excess quantities
- Future 30Y years use `daraByYear.get(year) ?? DARA` in the hypothetical-qty formula; a higher future-30Y DARA increases `future30yTotalCost` and therefore increases future 30Y cover excess quantities
- Bracket target funded-year qty is computed with the bracket year's own DARA from the map
- When `daraByYear` is null/absent, behaviour is identical to the scalar-DARA path

**This rule applies to the 2040 upper-bracket target and every 2041+ rung feeding its LMI
estimate, not just the funded years a caller sizes directly.** The gap-parameter calculation's
preliminary long→short sweep (which estimates the income arriving at 2040 from all rungs above it,
to size the 2040/gap bracket excess) and the 2040 target-quantity itself must read
`daraByYear.get(year) ?? DARA` per year, the same as everywhere else — not the scalar `DARA` alone.
Previously they did not: a stale scalar (e.g. the UI's top-level DARA box left out of sync with a
freshly-inferred per-year shape) would size the bracket excess for
a materially different, wrong income level even though the funded years below were sized correctly
from the real per-year map — leaving a large, uncorrected net-cash surplus after Run (traced case:
$21,417/yr real per-year target vs. a stale $41,500 scalar leaking into the bracket sizing produced
a $9,186 surplus; fixing the scalar sync alone got it to $1,031, fixing this rule is what makes that
$1,031-scale residual the general case regardless of UI staleness). The fix generalizes past this
one bug: *any* caller supplying a
non-flat `daraByYear` depends on the gap/bracket math actually reading it for every rung, not just
the ones it sizes by name.

### Per-Rung DARA Selection

> **Terminology note — LMI+AMD.** "LMI" (later-maturity interest) is now an incomplete name:
> when the ladder extends past 2056 the held excess 2052 TIPS contribute **accrued market
> discount (AMD)** that is credited to earlier years as interest. The income flowing downward
> in the sweep is therefore **LMI+AMD** (later-maturity interest *and* accrued market discount).
> The selection model below depends on this downward flow.

Earlier revisions of this app split the ladder at explicit, persisted **split years** into named
segments, each with its own Infer/constant-stamp UI in a separate DARA Plan card. That conflated
two distinct things: a convenience for assigning one DARA value to a run of rungs (genuinely
useful — see §Two Distinct Actions below), and a permanent boundary that had to be created,
persisted, exported, and re-detected from holdings holes. The boundary added ceremony without
adding information the CUSIP/Qty export doesn't already carry (the resulting per-year DARA values
*are* the durable plan). Split years, segments, the DARA Plan card, and the holdings-hole
auto-split detection have all been removed in favor of acting directly on the ladder table.

#### Selecting rungs

A **selection** is a transient set of funded years chosen directly on the ladder table (Rebalance's
`#simple-table` or Build's `#build-table`), never persisted beyond the current session:

- **Quick click** (press and release without moving off the row) on a funded-year group-header row —
  toggles that row's expand/collapse. It does **not** select — a plain click and a selecting click
  are different gestures, so the two never fire on the same press.
- **Press-and-hold** (~600ms without moving) on a group-header row — selects that one row (the anchor
  for a following Shift-click) and does **not** toggle expand/collapse.
- **Click-and-drag** across rows — builds a range selection live as the pointer moves. If the drag
  starts moving before the hold threshold elapses, it preempts the hold and range-selection begins
  immediately; if the hold has already fired (selecting the single starting row) and the pointer then
  keeps moving into other rows without releasing, the same drag continues to extend the range from
  there — a hold and a drag chain into one gesture rather than being mutually exclusive. Either way,
  the trailing click this produces is suppressed (it just finished selecting, not toggling
  expand/collapse).
- **Shift-click** a second row — selects the whole range between the last-clicked row (the anchor)
  and this one. If there is no anchor yet, a bare Shift-click selects just the clicked row as a new
  anchor; it does not toggle expand/collapse either way.

The selection drives a small toolbar (`#selection-toolbar`) that appears only while at least one
rung is selected, with a year-range label, a **Set DARA** input + Apply, an **Infer DARA** button
(Rebalance only — Build has no holdings to self-finance against), and a Clear button. Switching
mode (Build ↔ Rebalance) or loading a new file clears the current selection.

#### Two distinct actions

- **Set DARA** (`_runSelectionSetDara`, both modes) — stamps one typed value across every currently
  selected rung, marks each as manually pinned (`_daraManualYears`, same as a hand-typed per-rung
  edit), then **clears the selection**.
- **Infer DARA** (`_runSelectionInfer`, **Rebalance only**) — a scoped self-financing solve
  (`inferScaledDARAFromPortfolio`, `flat = true`) over the selected years alone, against current
  holdings, applied immediately with no separate Apply step, then **clears the selection**, same as
  Set DARA. Build has no holdings to solve against, so its toolbar never shows this button.

Both actions apply immediately and close the toolbar afterward — there is no lingering "applied but
still showing Apply" state to click through. `#selection-dara-input` is cleared on close too, so a
later selection never opens showing a stale value from a prior action. The solved/typed value stays
visible where it actually lives: in the per-year DARA panel's own rung rows once they re-render.

Both actions touch **only the selected rungs' DARA** — never any rung outside the selection. Both
also respect **manual-year pins** within that selection: a year the user typed directly into its
own row is held fixed even if it sits inside the current selection — its real cash flow still
counts toward the self-finance target, it just isn't a candidate for restamping.

#### No cascade — DARA is fixed once set, the rebalance absorbs the rest

LMI+AMD flows **only downward** (longer → shorter maturities), so changing a selection's DARA
changes the income arriving at every year below it. That change does **not** rewrite those years'
DARA. DARA is the user's stated target for that funded year (e.g. an RMD plan mapped out by
segment) and stays exactly as set; a shift in incoming LMI/AMD instead changes what Run buys or
sells for that year to still hit its own unchanged DARA. This is nothing new — it's the ordinary
2.0 sizing identity (`DARA_y = trade + incoming credits`) already applied at Run; Infer/Set DARA
just no longer reach past their own selection to pre-empt it.

One consequence: net whole-portfolio cash after a single scoped Infer is **not** guaranteed near
zero — only the selected segment's own net cash is (via `inCashScope`, below). Inferring every
segment of the ladder in turn (e.g. the top range, then the bottom range) does drive the whole
portfolio to ≈ 0, since the scopes' own net-cash targets union to cover everything; inferring only
one segment leaves the rest exactly as it was.

`inferScaledDARAFromPortfolio` (the scoped solve behind Infer DARA) is unchanged from its
scoped-segment form: `scopeYears` are solved for, years outside are held fixed at
`fixedDaraByYear`, and `pinnedDaraByYear` keeps manually-typed years fixed within scope while still
counting their cash flow. Trades outside `[firstYear, lastYear]` (e.g. the upper bracket when
`lastYear` lands inside the gap years) are still credited to whichever scope reaches
`lastYearOverride`, via `inCashScope`, same as before.

#### No edit history

There is no Undo or Revert-to-loaded — both were removed once editing became this cheap: a
selection covers as many rungs as needed in one action, so walking back a mistake is just making
another selection and setting or inferring the correct value, the same way the original edit was
made. `_daraLoadedSnapshot` (the as-loaded/imported map, per-mode) still exists internally, but only
to let `_daraPlanUnedited()` (§Funding the rebalance above) tell whether the store has been
touched since load; `_daraManualYears` records which rows the user hand-stated, so Run can pin them
against the self-financing scale. Neither drives a user-facing restore path.

#### Persisting a DARA plan across sessions

File load always **mirrors current holdings with no inference** (§Per-Year DARA from Portfolio), so
re-uploading a fresh broker export (Vanguard/Fidelity/Schwab) wipes any per-year shape set up on a
prior visit — brokers never carry the `#fundedYear,dara` block that makes *our own* exports
reproduce exactly (2.1 Broker Import), and a broker file has no DARA data of its own to overlay a
plan onto. The **standalone DARA-plan file** (2.1 §Standalone DARA-plan file — plain Export/Import
buttons in the main card, no CUSIP rows, no segments) is the durable, portable path for this:
export the current per-year shape once, and re-import it onto any subsequent fresh holdings upload,
on any device. There is no same-device localStorage cache anymore — it was found to surface an
unwanted "saved plan found" prompt on nearly every load and was dropped; the standalone file is now
the only persistence mechanism, used deliberately rather than offered automatically.

---

## Named Quantities & Formulas (Rebalance)

### Target CUSIP resolution (Within-Year Allocation Policy)

For each ordinary (non-bracket) funded year in Phase 4, before any of the quantity formulas below
apply, `rankForYear({ candidates: _canon.yearTipsListMap[year], held: yi.holdings, policy:
allocationPolicy, maturityPref, rankOverride: yearRankOverrides.get(year) })` (2.0 §Within-Year
Allocation Policy, 4.0 §allocation-policy.js) ranks that year's candidate ∪ held union, highest
buy/hold-priority first:

```
targetCUSIP = rank[0].cusip
nonTarget   = rank.slice(1), filtered to currently-held cusips
```

**Sell order (`'maturity'`/`'saYield'`, or an active per-year override).** `nonTarget`, in rank order
(least-preferred-to-hold first), is drained one at a time — fully settling each to zero before moving
to the next — until the year's own P+I `need` is met or `nonTarget` is exhausted; the drain **stops
the instant a CUSIP isn't fully drained**, leaving every less-preferred-to-sell CUSIP after it
(including `targetCUSIP`) untouched, even if one of them would arithmetically divide the leftover
residual more evenly (a smaller per-bond value one rank later must never let it sell ahead of an
earlier, still-held CUSIP). Only once every `nonTarget` member has been sold to zero does the
remaining `need` gap — deficit (buy) or a residual too small to be met by any further whole-bond sale
(shrink) — apply to `targetCUSIP`; while any `nonTarget` member still holds qty, that residual is
accepted as a small under/over-shoot rather than touching `targetCUSIP`. This is a straightforward
strengthening of the pre-feature rule (previously: always target the latest-maturing held CUSIP,
always sell oldest-held-first) — the same one-CUSIP-at-a-time drain shape, now driven by a ranking
that reflects the active policy instead of a hardcoded direction.

**Sell order (`'equal'`)** does not drain one CUSIP fully before the next — see 2.0 §Within-Year
Allocation Policy's `levelValues()` description. The target/non-target pool (the same set as above)
is leveled toward equal value together: growth water-fills the currently-lowest-value member(s),
shrinkage drains the currently-highest-value member(s), re-leveling at every step. `targetCUSIP`
still absorbs whatever rounding remainder is left once every other pool member lands on its leveled
value — the same remainder-absorption pattern §Multi-TIPS Funded-Year Split already uses for a
build's last sub-rung.

**Scope — excess vs. the year's own funded need.** A bracket year's excess CUSIP is never chosen by
`rankForYear`: it is identified by §Bracket Identification Rules / 2.0 §Duration Matching, and a
rebalance never buys a retained maturity into it beyond that duration-matched target ([Retained
Bracket Excess](../../knowledge/DATA_DICTIONARY.md#retained-bracket-excess)). But a bracket year that
is also a [Funded Year](../../knowledge/DATA_DICTIONARY.md#funded-year) (`bracketYear ∩ Funded Year`,
2.0 §Retained Bracket Excess) has its own, separate funded-year need, and *that* role is not fixed to
the excess CUSIP — it is resolved by the same `rankForYear` call an ordinary year uses, restricted to
`policy: 'maturity'` (maturityPref's own direction only; the ordinary-year `allocationPolicy` axis
— `'equal'`/`'saYield'` — has no bearing on which maturity a bracket year funds, since it is not an
ordinary year's cash-flow-preference question). Under the default `maturityPref='last'` this always
resolves to the same CUSIP as the excess target — the [Active Lower Bracket](../../knowledge/DATA_DICTIONARY.md#active-lower-bracket)
*is* "the most recently issued 10-year TIPS," so ranking by latest-maturity agrees with it by
construction — so today's behavior is unchanged for every user who has not touched Maturity
preference. Under `'first'`/`'semiannual'`/`'all'`/`'select maturities…'`, the funded need can resolve
to a *different* maturity in that same bracket year than the one carrying the excess (e.g. funding
January while excess stays on the active July maturity) — the year splits into two rows, priced off
their own CUSIPs independently, never a simultaneous same-year buy+sell of one maturity to fund the
other. A same-maturity-year retained maturity ([Retained Lower Bracket](../../knowledge/DATA_DICTIONARY.md#retained-lower-bracket),
ruling 4, `RETAINED_BRACKET_TODO.md`) is a candidate in this ranking like any other held maturity in
the year, not excluded from it — under `'first'` it is very often the one maturityPref actually wants
funding the year's need, since it already matures earlier than the active/canonical maturity (the
recurring real-world shape ruling 4 itself describes: an older maturity held from before the active
bracket rolled forward). When it wins, its own excess share is unaffected — still solved by
`bracketWeightsN`, still frozen, still never increased by a rebalance — and combines with the funded
quantity onto that one row, so ruling 4's separate placement step is skipped for that specific
maturity (its final quantity is now owned by this resolution instead) while any OTHER same-maturity-year
retained maturity, not chosen as this year's funded CUSIP, still gets ruling 4's unconditional
earliest-maturity-first treatment untouched. Sell order among whatever ELSE a bracket year holds
(neither the excess CUSIP nor its own funded CUSIP) is still the same maturity-direction rank, which
reduces to the existing oldest-first rule in 2.0 §Retained Bracket Excess whenever `maturityPref='last'`.

Gap/future30y-only years (no funded role at all) are unaffected — there is no funded need to resolve,
so only the excess CUSIP applies, exactly as before.

**Funded-Year Quantity Before, raw estimate** (`fundedYearQtyBeforeRaw_b`, held by year in `src/rebalance-lib.js#bracketTargetFundedYearQtyBefore`):
```
fundedYearQtyBeforeRaw_b = ROUND((DARA − LMIFromAbove_Before − nonTargetPI_b) / piPerBond_b)
```
`LMIFromAbove_Before` = sum of annual interest from all actual holdings in years maturing after year b (includes both funded and excess holdings at those years). `nonTargetPI_b` = P+I from non-target CUSIPs held in the same bracket year. Future30y cover years are excluded — treated as pure excess (`fundedYearQtyBeforeRaw_b = 0`). This formula is applied to ALL standard gap bracket years regardless of CSV format. This is a pure holdings-interpretation estimate — it must never reference `fundedYearQtyAfter` or any other after-side target — but it is a *raw estimate*, not yet the displayed Quantity Before (see reconciliation below).

**Excess Quantity Before, raw estimate** (`excessQtyBeforeRaw_b`):
```
excessQtyBeforeRaw_b = MAX(0, qtyBefore_b − fundedYearQtyBeforeRaw_b)
```
Funded-first rule: apply the funded-year claim first; the remainder of the held quantity is the current excess. This *derives* the before-split rather than reading the import's excess column back verbatim — but the **imported excess is not discarded**. It still does its two jobs: in duration matching it is the cover/bracket excess (the recorded build intent), and in funded sizing its annual interest and realized AMD count as later-maturity interest (`LMIFromAbove_Before` above counts both funded **and** excess holdings). The *size* of the imported cover excess is also what recovers the intended first and last funded year (see 2.0 §Gap Year Coverage Model and §Future 30Y Rungs). Because a correctly-built ladder's derived split reproduces its own imported excess, a build→export→import→rebalance round-trip yields no trades. For Future-30Y cover years: `excessQtyBeforeRaw = ROUND(future30yTotalCost × weight_b / costPerBond_b)` (cover-pair cost split).

**Excess Quantity After** (`excessQtyAfter`):
```
excessQtyAfter_b = ROUND(totalBlockCost × weight_b / costPerBond_b)
```
Post-rebalance excess for duration matching. `costPerBond = price/100 × indexRatio × 1000` (per **TIPS Basics**).

**Reconciling the raw split with the after target (no same-maturity buy+sell).**
`fundedYearQtyBeforeRaw_b`/`excessQtyBeforeRaw_b` above are computed purely from holdings — they never reference
`fundedYearQtyAfter`/`excessQtyAfter`. But they are independently-derived *estimates*, using the before-side
LMI cascade and this year's own before-side DARA math, and routinely disagree with the after-side
targets (`fundedYearQtyAfter_b`, `excessQtyAfter_b`) even at an otherwise-untouched bracket year — rebalancing
elsewhere in the ladder shifts the later-maturity-interest cascade feeding this year's own funded
formula, and a custom per-year DARA plan can push the after-side funded need further still. A bracket-
target CUSIP's funded and excess portions are the *same held maturity*, so a plain per-bucket
subtraction of the raw estimate from After is only wrong when the two buckets' naive deltas point in
*opposite* directions — one bucket would buy this maturity while the other sells it, a nonsensical
simultaneous buy+sell of one bond. When both buckets move the same direction (or one doesn't move at
all), the raw estimate already reconciles `fundedYearQtyBeforeRaw_b + Δ = fundedYearQtyAfter_b` per bucket on its own
and passes straight through unchanged:

```
naiveFundedDelta_b     = fundedYearQtyAfter_b − fundedYearQtyBeforeRaw_b
naiveExcessDelta_b = excessQtyAfter_b − excessQtyBeforeRaw_b

if naiveFundedDelta_b and naiveExcessDelta_b have OPPOSITE signs:
    realloc_b        = MIN(|naiveFundedDelta_b|, |naiveExcessDelta_b|)   // signed toward whichever is buying
    fundedYearQtyBefore_b     = fundedYearQtyBeforeRaw_b + realloc_b       (moving toward the buying bucket)
    excessQtyBefore_b = excessQtyBeforeRaw_b − realloc_b
else:
    fundedYearQtyBefore_b     = fundedYearQtyBeforeRaw_b
    excessQtyBefore_b = excessQtyBeforeRaw_b

fundedYearQtyDelta_b     = fundedYearQtyAfter_b − fundedYearQtyBefore_b
excessQtyDelta_b = excessQtyAfter_b − excessQtyBefore_b
```
Relabeling currently-held units between the two buckets costs nothing and trades nothing — only the
opposing portion is netted out this way; before pricing any trade, as much of what's already held as
will cancel the conflicting signs is relabeled first, and only the remaining shortfall or leftover is
actually bought or sold. **`fundedYearQtyBefore`/`excessQtyBefore` — the reconciled values, not the raw
estimates — are what render as the Quantity Before / Cost Before / Excess Amount Before columns.**
Showing the raw estimate instead (e.g. displaying `fundedYearQtyBeforeRaw_b` as "Quantity Before" while the
delta is computed against the reconciled `fundedYearQtyBefore_b`) was tried and rejected: it reintroduces the
exact arithmetic break this reconciliation exists to prevent (Before + Delta ≠ After on-screen), since
whatever number is *displayed* as Before must be the same number the delta is computed against. In the
(usual) same-direction case the two coincide exactly, so nothing changes there — this only alters the
displayed split in the rarer opposite-sign case, and even then only by relabeling units already held
(the total `qtyBefore_b` is untouched).

This guarantees three invariants always hold: (1) `fundedYearQtyDelta_b` and `excessQtyDelta_b` are never
opposite in sign for the same CUSIP; (2) `fundedYearQtyDelta_b + excessQtyDelta_b = qtyAfter_b − qtyBefore_b`
(the reallocation only changes how the total trade is split between the two buckets, never the total
itself); (3) `fundedYearQtyBefore_b + fundedYearQtyDelta_b = fundedYearQtyAfter_b` and `excessQtyBefore_b + excessQtyDelta_b =
excessQtyAfter_b` — Before + Delta = After reconciles per bucket, not just in total.

**Lower bracket priority rule** (`bracketWeightsN`, 4.0 §Computation Modules; replaces the plain
2-bracket formula across all the lower brackets; see 2.0 §Retained Bracket Excess). Retained
maturities are **not** priced as though they sat at the active lower bracket's duration — each is
frozen at its own (shorter) duration, and only the active lower bracket and upper bracket are
solved for, from the duration-match equation with the retained lower brackets as fixed inputs:

```
R   = Σ (retainedExcessCost_r) / totalBlockCost
D_R = Σ (retainedExcessCost_r / totalBlockCost) × dur_r

upperWeight       = ((dGap − D_R) − (1 − R) × dur_activeLower) / (dur_upper − dur_activeLower)
activeWeight = (1 − R) − upperWeight
```

**`activeFloorWeight` — the active lower bracket's floor.** The active lower bracket is "the only
lower bracket a rebalance buys" ([Active Lower Bracket](../../knowledge/DATA_DICTIONARY.md#active-lower-bracket))
— it is never sold to make room for a duration match the way a retained lower bracket may be. `bracketWeightsN`
takes `activeFloorWeight` (default 0): the active bracket's own currently-held excess, as a share of
`totalBlockCost`. Every trigger and target below that reads "0" is really this floor — a plain 0 only
when the active bracket currently holds no excess of its own, which is why every pre-existing caller
that omits `activeFloorWeight` reproduces the exact original math unchanged.

**Why this matters (traced case).** A large, short-duration retained lower bracket can pull the *unconstrained*
solve's `activeWeight` down toward — but not past — literal zero without ever going negative. The
original rule ("sell retained only when `activeWeight` solves negative") missed this: the active
bracket's excess was treated as pure target cost and sold down to whatever the unconstrained weight
computed, which could be **less** than what it already held — backwards from the intent. A real
2034/2036 portfolio hit this exactly: Jan 2034 (retained, duration 6.93, holding a disproportionately
large lower bracket excess share) computed an active-bracket weight of ~3×10⁻¹⁶ (float noise for exactly
0, never negative) for the active lower bracket, so the "over-allocated, sell retained" branch never
triggered — the code instead took the "under-allocated, freeze retained" branch and sold 2036's excess
from 36 bonds to 0 while 2034 kept 75 of its 83. Fixed by flooring the solve at `activeFloorWeight`.

If `activeWeight` solves below `activeFloorWeight` (**over-allocated** — the lower brackets are
carrying more than the gap years need, even leaving room for the active bracket's own current holding):
- Sell the **earliest** [retained bracket excess](../../knowledge/DATA_DICTIONARY.md#retained-bracket-excess)
  maturity, and **only as much of it as restoring the match requires** — solve for the exact
  weight of that bracket that brings `activeWeight` back to exactly `activeFloorWeight`, holding
  every other retained lower bracket fixed, and clamp to `[0, currently held]`. Selling a bracket out entirely when a
  partial sale would do is needless trading, not the rule.
- Only if depleting that bracket **entirely** is still not enough does the next-earliest retained
  maturity get the same treatment.
- Retained excess is **never bought**; the active lower bracket and upper bracket absorb whatever
  the retained lower brackets no longer cover — the active bracket never below its own floor.

If `activeWeight` solves at or above `activeFloorWeight` on the first pass (**under-allocated**):
- Freeze every retained maturity at its current excess (never increased): `targetCost_b = excessCost_b`
- Buy the active lower bracket to fill the remainder: `targetCost_activeLower = totalBlockCost × activeWeight`
  (never below `totalBlockCost × activeFloorWeight`, i.e. never a net sell of the active bracket).

For each lower bracket `b`: `excessQtyAfter_b = ROUND(targetCost_b / costPerBond_b)`

With no retained maturities (`R = 0`) this reduces to the plain 2-bracket case: the active lower
bracket is sized straight from `bracketWeights(dur_activeLower, dur_upper, dGap)`.

**Round-trip rounding note** (2.0 §Retained Bracket Excess): the over-allocated partial-sell target
is a continuous dollar figure; `ROUND()`-ing it to a whole bond can land the holding a few dollars
short. Exporting and reimporting that portfolio unchanged can therefore ask for a very small
follow-up trade — up to about one bond, always bought into the active lower bracket, never back
into retained excess — on the very next rebalance. Accepted as an ordinary whole-lot-investing
artifact, not a bug; the test suite tolerates it (≤1 bond of churn) rather than requiring
bit-perfect round-trip idempotency in this specific regime.

**Cash Delta** (`cashDelta`, `costDelta`):
```
cashDelta = -(qtyDelta × costPerTips)
  where qtyDelta = qtyAfter - qtyBefore
```
Positive = cash received (net sell); negative = cash outflow (net buy). At a bracket-target CUSIP,
the funded-row and excess-row Cash Deltas use `fundedYearQtyDelta`/`excessQtyDelta` (the reallocated split
above), not a raw `fundedYearQtyAfter − fundedYearQtyBefore` / `excessQtyAfter − excessQtyBefore` subtraction — the
latter can manufacture a same-CUSIP buy+sell pair. The row total (`qtyAfter − qtyBefore`) is
unaffected either way; only its split between the two rows changes.

---

## Bracket Identification Rules

The active lower bracket comes from `tipsMap`; retained maturities come from holdings.

### Active Lower Bracket (Canonical)
The [active lower bracket](../../knowledge/DATA_DICTIONARY.md#active-lower-bracket) is **always** the most recently issued outstanding 10-year TIPS from `tipsMap` maturing below `minGapYear` — the longest-dated 10-year maturity available. Holdings are not consulted. This is the same search used for `anchorBefore` in gap parameter calculation. When **Retain lower bracket excess** is off, older holdings that previously served as the lower bracket are no longer in `bracketYearSet`; Full rebalance rebuilds them to funded-year qty only, selling any excess.

### Retained Maturities (Holdings-Based)
When **Retain lower bracket excess** is on, `identifyBrackets` (`rebalance-lib.js`) picks a **single** orig-lower CUSIP from holdings, older than the active lower bracket:
1.  **CUSIP Aggregation**: Sum total quantity of each CUSIP across the entire portfolio.
2.  **Excess ARA Priority**: The primary metric is **Excess ARA** (`araByYear[y] - DARA`). Among candidate years, the **single** year with the highest Excess ARA is picked as the orig-lower retained maturity. **Note**: "Excess ARA" is a one-time empirical probe used solely here — not a general concept.
3.  **Tie-Breaking**: Among candidates with equal Excess ARA, pick the CUSIP with the highest aggregate quantity.
4.  **Range**: Only years in `[LOWEST_LOWER_BRACKET_YEAR (2032), minGapYear)` are considered.
5.  **Fallback**: If no candidate year is found (e.g. `firstYear` inside the gap), only the active lower bracket carries lower bracket excess — the lower brackets reduce to the plain 2-bracket case.

**A second, always-checked source: a holding in the active lower bracket's own maturity year.** A
maturity year may hold more than one TIPS (DD §Bracket Year TIPS), and the year housing the active
lower bracket is no exception — most commonly an earlier-maturing TIPS held from before that year's
latest issue became active. `rebalance-lib.js` checks the active lower bracket's own maturity year
for any OTHER held CUSIP maturing there, independent of the Excess ARA pick above (the two sources
can both fire at once — one cross-year retained maturity plus one sharing the active bracket's own
year — or either alone). That holding's own funded-year need is computed independently, crediting
nothing forward from the active CUSIP (ruling 4, RETAINED_BRACKET_TODO.md): whatever it holds beyond
that need is retained excess at its own duration, entering the same solve as any other retained maturity.
Without this, `identifyBrackets`'s year-only comparison against the active lower bracket's own
maturity year could not tell "a genuinely different, older bond that happens to mature in the same
year as the active one" apart from "the active bond itself" — collapsing to the plain 2-bracket case
and either buying more of the older bond (when it was the only retained candidate) or selling it down
to fund the year with no need to (when a cross-year retained maturity already filled that role).

**Currently limited to one cross-year generation, plus the same-maturity-year case above.**
Conceptually any number of retained maturities may accumulate across DIFFERENT years as the active
lower bracket rolls forward — each rebalance buys the then-active lower bracket, and when a later
maturity is issued the previous one becomes retained, so the count grows by one with each pre-gap
issuance — and `bracketWeightsN` (2.0 §Retained Bracket Excess) is written to solve for any number of
them. But `identifyBrackets`'s Excess ARA step above only ever returns one CROSS-YEAR orig-lower
CUSIP, so only the single most-recent cross-year retained generation is fed into the solve today;
identifying and ordering multiple cross-year retained generations from holdings is a known gap, not
yet implemented (see branch `retained-bracket-work` for a prior, reverted attempt and why it broke on
real holdings). Every retained maturity found — the one cross-year pick and the same-maturity-year one —
are sorted earliest-maturity-first and depleted in that order if the match is otherwise unsolvable
(ruling 5, RETAINED_BRACKET_TODO.md; 2.0 §Retained Bracket Excess).

---

## Rebalance Algorithm Flow

This is *the* rebalance algorithm (there is one rebalance — the "Gap-only" mode it was once named for was scrapped; see §Purpose). It runs the full ladder including the gap brackets, and is driven by the **per-year DARA targets**, which may be uneven (2.0 §Duration Matching).

### Phase 1: Derive Parameters
- Settlement date, RefCPI, per-year DARA targets.
- Build holdings ladder and identify official range.
- **DARA shape**: the per-year targets come from the panel store (mirror at load, or user-reshaped). For the default mirror, Run applies the **shape-preserving self-financing scale** (see §Funding the rebalance) so the executed targets net to non-negative cash; an explicitly reshaped store / our-export DARA block is used as-is.
- Identify current gap years.

### Phase 2: Gap Year and Future 30Y Parameters
- Calculate **Gap Avg Duration** and **Gap Total Cost** (see **2.0 TIPS Ladders**).
- If `lastYear > 2056`, calculate **Future 30Y Avg Duration** and **Future 30Y Total Cost**.

### Phase 3: Bracket Resolution
- Identify lower/upper bracket CUSIPs from holdings per §Bracket Identification Rules.
- Calculate modified duration for each bracket.
- Solve weights using **2.0 TIPS Ladders**; apply the lower bracket priority rule when retained bracket excess is present.
- If `lastYear > 2056`, identify Future 30Y cover pair and solve weights.

### Phase 4: Ladder Rebuild (longest to shortest)
Process ALL years in [firstYear, lastYear] **longest to shortest**.
- **Bracket years**: use BEFORE-state LMI pool to ensure `fyQty` remains constant.
- **Rebalance years**: use running post-rebalance LMI pool.
- Apply bracket excess quantities on top of target `fyQty`.
- **LMI pool rule**: Process according to **2.0 TIPS Ladders §Algorithm**. Excess bracket holdings are actual bonds — their interest reduces funded-year quantities of all shorter rungs, on equal footing with funded-year holdings.

---

## Excess Balance Check

A **diagnostic** of how well the *current* holdings pre-funded the gap coverage — **not** the funding invariant. The funding invariant is **net cash (`costDeltaSum`) ≥ 0 and small**, guaranteed by the self-financing scale (§Funding the rebalance).
```
1. Previous excess cost
2. Cost to fill rebalance years
3. Gap theoretical cost

Surplus/(Shortfall) = 1 - 2 - 3
```
Near-zero indicates the portfolio already held the bracket excess that the gap requires. **Large negative is expected for a basic file** (broker/legacy CSV) that was never built by this app and so never pre-funded the gap — it simply means the rebalance must buy that excess now. The rebalance still self-finances: the shape-preserving scale sells the funded rungs down to raise the cash, so `costDeltaSum` lands small/non-negative even when this surplus is deeply negative. Read this check as "did the prior portfolio carry its gap coverage," and read `costDeltaSum` as "is the rebalance self-financing."
