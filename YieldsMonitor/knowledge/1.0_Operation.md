# 1.0 Yields Monitor Overview

## Purpose
The Yields Monitor provides real-time and historical visualization of Treasury (Nominal) and TIPS (Real) yields. It is designed for intraday precision and long-term trend analysis.

## Data Architecture

**2D, 10D ranges**: Fetch intraday data from CNBC GraphQL API (`1D` and `5D` timeRange). These are the only two ranges with no other fallback (every other range falls back to the R2 daily baseline) — if CNBC's live feed returns nothing at all for a symbol (a real outage, confirmed 2026-08-22 for all five TIPS symbols simultaneously while Nominal Treasuries were unaffected; confirmed again 2026-08-24, this time affecting both TIPS and Nominal symbols together, with the feed frozen since the prior weekend's overnight session open; there's no way to distinguish this from an off-hours quiet period from an empty response alone), the app walks backward through the `intraday-raw` archive (see R2 stores table below) looking for the most recent day that has the requested feed, and uses that single day's snapshot rather than showing nothing. The result is tagged with the archive's own `fetchedAtET` field (when `archiveIntraday.js` actually ran) rather than the snapshot's last bar's own timestamp — CNBC's chart-bar feed can append one trailing element whose value is the genuine live quote but whose own timestamp field is mislabeled as the last real per-minute bar's time (see the "TIPS latest"/"Treasuries latest" entry below), so trusting a bar's own timestamp here would understate how current the fallback actually is. The status labels (below) reflect this `fetchedAtET` value when a fallback is used.

**1Y, 2Y, 3Y ranges**: Reread the CNBC `6M` daily feed fresh on every load — the same feed for all three UI ranges — filtered client-side to the selected cutoff (1/2/3 years). CNBC's `6M` timeRange is unreliable per-symbol: on any given load it can return an empty payload for an unpredictable symbol (observed rotating across different symbols on different days, not fixed to one) — when that happens, the app falls back to `3M` then `1M` (shallower daily-resolution tiers of the same feed, ~2yr and ~1yr back respectively, not literally "3 months"/"1 month" despite the parameter name). The result is cached per symbol (shared across 1Y/2Y/3Y, since it's the same underlying feed), but the cache is depth-checked against the requested range's cutoff on every use, not reused blindly — a shallow fallback cached while viewing 1Y/2Y is re-attempted against the deeper tiers when the user switches to 3Y, so a symbol stuck on the `3M` fallback isn't left silently truncated at ~2 years forever. If even `1M` still doesn't reach the requested cutoff (a symbol's CNBC tiers are all short that day), the gap older than CNBC's earliest bar is filled from the same R2 `history.json` baseline the 10Y/ALL branch below uses — reliable, but weekly/quarterly resolution, so the line goes coarser before that point rather than truncating. The cached daily-tier result can also be non-empty overall yet still miss its own most recent day (observed 2026-08-22: `US5YTIPS`'s `6M` tier had bars through mid-2025 then jumped straight to the current day, skipping the most recent trading day entirely, while its own `3M` tier did have it) — the tier waterfall above only retries a shallower tier when the deeper one is completely empty, not merely missing its tail, so it wouldn't catch this. The latest live point from CNBC's `5D` feed is appended on top regardless, same "baseline + live tip" pattern as Custom range and 10Y/ALL below, so the most recent point is available here too rather than only in ranges that read it live some other way.

**10Y, ALL ranges**: Fetch the persistent daily-close baseline from R2 (`Treasuries/yields-history/history.json`, a single nested-by-symbol file) — refreshed by `updateYieldsHistory.js` — plus the latest live points appended from CNBC's `5D` feed, same "baseline + live tip" pattern as Custom range, so the most recent point is available here too rather than only in ranges that reread CNBC live. R2 CORS is live for this origin; see repo-root `knowledge/Data_Pipeline.md` §3.1.

**Custom range**: R2 baseline plus latest live points appended from CNBC's `5D` feed, to ensure "Latest Yield" and "Day Change" reflect current market data.

**If R2 fetch fails**: Returns null; app displays "No data available." (Transient/network failure — R2 CORS itself is configured and working as of 2026-08-03, see `Data_Pipeline.md` §3.1.)

**"TIPS latest" / "Treasuries latest" status labels (bottom-left)**: Two separate readings, not one combined value — TIPS and Nominal Treasuries can genuinely diverge in freshness (confirmed 2026-08-22: a CNBC outage affecting only TIPS symbols while Nominal Treasuries stayed live), and a single combined label would either overclaim freshness for the current group or needlessly undersell the one that isn't stale. Each is sourced from the same batched `quote.cnbc.com/quote-html-webservice/restQuote/...` REST quote service call `fetchLatestQuotes()` makes for the sidebar yield table (CORS-open; `US10YTIPS`'s `last_time` for the TIPS reading, `US10Y`'s for the Treasuries reading) — not from the chart-bar feed's own per-bar timestamps. CNBC's chart-bar (`priceBars`) feed can carry a stale per-bar `tradeTime` — the feed appends one trailing element beyond the normal per-minute bars (recognizable by `%`-suffixed OHLC strings, a real day-range O/H/L/C instead of a flat single-tick bar, and `volume: null` instead of `"0"`) whose *value* is the genuine live quote but whose own timestamp field can be mislabeled as the last real per-minute bar's time rather than when it was actually fetched (confirmed 2026-08-21: archived at 17:05 ET per `archiveIntraday.js`'s own clock, but that trailing element's own timestamp still read 10:54, the time of the last real per-minute bar before a gap in the minute-by-minute series) — so it isn't trustworthy for this label. The `last_time` field from the quote service doesn't have that problem and is fetched fresh on every `fetchAllData()` run (page load and Refresh Data); if that fetch fails, the label falls back to the chart feed's own last-bar time for that group. This label sits directly under the sidebar yield table and tracks the same live-quote freshness the table shows — it does **not** get overridden when a symbol's chart falls back to the `intraday-raw` archive for 2D/10D (confirmed 2026-08-24: the label correctly showed a live time while the chart itself displayed an archived prior-weekday snapshot; an earlier version of this label used to show the archive's own timestamp instead in that case, which read as stale/wrong next to the now-live sidebar values and was removed). Individual chart hover-tooltips still read the chart-bar feed's own (possibly stale) per-bar `tradeTime`, matching whatever cnbc.com's own chart displays for that same bar — only the status labels and sidebar table are quote-service-first.

**Stale-data bridge on 2D/10D charts**: When a symbol's chart-bar feed is frozen or has fallen back to an archived day (as above) while its live quote (the same `fetchLatestQuotes()` reading the sidebar table and status labels use) is meaningfully newer — more than 30 minutes past the chart's last plotted point (`STALE_GAP_MS` in `app.js`) — the chart draws a dashed horizontal line from that last point out to the current time, at the last point's own yield level. This makes the gap between what the chart is showing and how current that actually is visible on the chart itself, rather than only in the separately-sourced sidebar/status readings. The chart's default view (`xMaxAnchors`) also extends out to "now" in this case (instead of stopping just past the last real point) so the dashed bridge is visible without the user having to pan/zoom to find it. The line is flat (not interpolated toward the live quote's own value) since no actual intraday data exists for that stretch — it marks elapsed time, not a real yield path.

**No local fallback code**: App fetches only from remote sources (CNBC, R2).

### R2 stores (under `Treasuries/yields-history/`)

| Store | Key pattern | Written by | Purpose |
|---|---|---|---|
| **Daily-close baseline** | `history.json` (nested by symbol) | `updateYieldsHistory.js` | Long-range/curve baseline — one daily 3PM close per symbol, all 14 symbols in one file |
| **Intraday-raw archive** | `intraday-raw/{symbol}/{YYYYMMDD}.json` | `archiveIntraday.js` | Immutable daily snapshot of the raw `1D`+`5D` feeds per symbol — originally for offline inspection of any past close window (Goal A audit), now also the live last-resort fallback for 2D/10D when CNBC's feed itself returns nothing (see 2D/10D description above) |
| **Close-timing probe log** | `close-probe/{symbol}.csv` | `probeClose.js` | Temporary investigation log of the last `1D` bar over time, to pin down when the 17:05 bar posts |

### Scheduled jobs (Windows Task Scheduler — `scripts/setup-windows-tasks.ps1`)

| Task | When (ET) | Script | Role |
|---|---|---|---|
| `YieldsHistory` | weekdays 17:00 | `updateYieldsHistory.js` | Refresh consolidated daily-close baseline |
| `IntradayArchive` | weekdays 17:05 | `archiveIntraday.js` | Capture raw 1D+5D feeds (all 14 symbols) |
| `CloseProbe` | weekdays 17:05, +15min ×1h | `probeClose.js` | Log US10YTIPS last 1D bar to find reliable 17:05 post time |

### Symbol reliability

The **US5YTIPS (5Y TIPS) feed is flaky** — sparse, irregular bars. Do **not** depend on it for close timing or as a reference symbol. Use **US10YTIPS (10Y TIPS)** as the reliable reference. The archive still captures all 14 symbols; this caveat governs which symbol any *logic* should anchor to.

## Tab Architecture
The monitor is organized into three analysis views:
- **Time Series**: Individual maturity charts with intraday shading and historical trending. (See 2.1). The TIPS charts additionally offer a Seasonally Adjusted (SA) yield overlay. (See 2.4)
- **Yield Curves**: Snapshot comparison of curves at the start vs. end of the range. (See 2.2)
- **Breakeven Inflation**: Visualization of market-implied inflation expectations. (See 2.3)

## Symbol List — Yield & Change Display

The sidebar symbol list shows the **latest yield and day change for every available security**, regardless of whether it is selected (checked) or not. Data is fetched for all symbols on every update; the yield/change display reflects that data for all entries. Only the charting area is gated on selection.

**Latest yield always comes from the live CNBC quote service** (`quote.cnbc.com/quote-html-webservice/restQuote`, `last` field — batched for all symbols in one request via `fetchLatestQuotes()`), not from the chart-bar feed. This is deliberate: the chart-bar feed (`webql-redesign.cnbcfm.com/graphql`) can go stale or freeze during a CNBC outage (confirmed 2026-08-22, TIPS only; confirmed 2026-08-24, both TIPS and Nominal symbols simultaneously) while the quote service keeps updating — same reasoning already used for the "TIPS latest"/"Treasuries latest" status labels. The chart-bar feed's own last point is used only if the quote fetch itself fails for that symbol. Charts (the plotted lines) are unaffected by this — they still use the archive-fallback mechanism described above for 2D/10D, or whatever range-appropriate historical data is available; only the sidebar's single "current value" reading is quote-service-first.

## Yield Change Calculation

The "day change" shown next to each symbol (e.g. `−0.016%`) is calculated as:

```
change = latest_yield − closeP.yield
```

where `latest_yield` is the live quote value described above (or the chart feed's last point if the quote fetch failed).

**`closeP` selection** — walks backward through `calculationData` (prefers `_5D` cache, then the 2D view's own `_5Dtip` cache — the same full 5D fetch under a different key, see `fetchOne` — before falling back to `_1D`), excluding `synthetic` points (see below), to find the last bar from the previous trading day where the bar timestamp is at **17:05 ET or earlier** (condition: `hour < 17 || (hour === 17 && minute <= 5)`). This anchors the reference to the actual end-of-session consolidation close — the same 17:05 basis the 2D/10D charts themselves already render (see `Close_Price_Investigation.md` §5), rather than the ~3 PM basis long-range views use.

**Why `_5D`/`_5Dtip` over `_1D` matters here** — CNBC's `1D` provider range is a rolling window (confirmed spanning as little as ~1 day at times, not a fixed prior-close-to-now span), and it can slide forward far enough — observed over a weekend — to no longer contain the previous trading day's actual daytime session, only its overnight tail. A `closeP` walk restricted to `_1D` in that state finds no bar satisfying the close condition, falls through to the `previous_day_closing` fallback below, and on a weekend that field can equal the current quote itself (both reflecting the same last trade) — producing a `0.000%` day-change for every symbol. `_5D`/`_5Dtip` span ~10 days and reliably still contain the previous day's real close.

The tooltip shows: `Since MM/DD/YYYY, 17:05 ET close (X.XXX%)`.

**Excluding synthetic points from the day boundary** — a bar whose raw `close` value carries a trailing `%` (every real bar's close is a bare numeric string) is a synthetic trailing live-quote tick, not a real per-minute bar, and is tagged `synthetic: true` during parsing (`fetchLive` in `app.js`). Its own timestamp is unreliable: the weekday form mislabels it with a stale prior timestamp (see the "TIPS latest"/"Treasuries latest" entry above), and a weekend form (confirmed 2026-09-12) instead rolls it forward to the current wall-clock day and time despite no trading having occurred that day. If left untagged, that weekend form makes the walk-back treat the actual last trading day as "the previous day" and anchor `closeP` to a bar within that same day's own session — producing a day-change reading of only a few thousandths of a percent instead of the real prior-day delta. The `closeP` walk and the `latestDayET` boundary it walks back from both use only non-synthetic points; the live quote (`currentY`) is unaffected, since it already comes from the quote service (or, as a last resort, the chart feed's true last point, synthetic included) rather than from this walk.

**Fallback when `closeP` is unreachable** — if the chart-bar feed has no data at all for the prior trading day (e.g. frozen during an outage), day change instead uses the quote service's own `previous_day_closing` field: `change = latest_yield − prevClose`. The tooltip in this case reads `Since prior close (X.XXX%, CNBC settlement)`, distinguishing it from the normal 17:05 ET close reference. If neither is available, day change shows `---`.

### Deviation from CNBC

CNBC's displayed yield change uses a different reference: the `previous_day_closing` field from their Tradeweb quote service. This value reflects an **earlier settlement quote** — apparently a mid-afternoon official price (~14:00–15:51 ET range on observed days) — rather than the 5 PM trading close.

**Example (2026-04-08, US1Y)** — predates the 2026-09-12 change from a `≤17:00` to a `17:05` reference (see `Close_Price_Investigation.md` §5/§8), so the "This app" row below reflects the older `≤17:00` anchor rather than the current one:
| Source | Reference time | Reference yield | Change vs. 3.677% |
|---|---|---|---|
| This app (at the time) | ≤17:00 ET | 3.693% | −0.016% |
| CNBC | ~15:00 ET (Tradeweb settlement) | 3.685% | −0.008% |

The discrepancy is expected and intentional regardless of which trading-close reference the app uses: CNBC's is an earlier official settlement publication (likely Tradeweb's intraday closing quote, distinct from end-of-day trading).

---

## Intraday Feed Structure & the 17:05 Consolidated Close

The CNBC `1D` and `5D` chart feeds are **24-hour continuous** (electronic/overnight trading), but each weekday has a clear day-session structure. Charts shade 08:00–17:00 ET as market-open; outside that is after-hours. Empirically (verified across all 14 symbols, every weekday in the `intraday-raw` archive):

1. **Continuous day-session trading** runs to roughly 16:56–17:04 ET (cadence is liquidity-driven; illiquid symbols produce fewer bars).
2. **One consolidated close bar per symbol is timestamped exactly `17:05` ET.** This is a **fixed-time end-of-day consolidation, not a random last trade** — it appears for every symbol every weekday regardless of liquidity, even when continuous trading stopped minutes earlier, and even after a `17:04` bar for liquid symbols. Drift from the last continuous bar is sub-bp (0–0.5bp), i.e. a settle value, not a new move.
3. **A ~2.5-hour session break follows** (17:05 → ~19:30 ET), after which the overnight electronic session resumes.

### The 17:05 bar is the session close — but NOT CNBC's *daily* close

> **Critical:** the `17:05` bar is the actual end-of-**session** trading close, but it is **not** the value CNBC stores as the **daily** close. CNBC's daily close (what the history files are built from) is the **~3 PM benchmark** — see *Daily-close basis* below and the full analysis in **[`Close_Price_Investigation.md`](./Close_Price_Investigation.md)**.

The `1D` feed is authoritative for the 17:05 session close:

| Feed | Resolution | 17:05 representation | Authoritative? |
|---|---|---|---|
| `1D` | 1-min | clean `17:05` consolidated timestamp | **Yes** — use this |
| `5D` | 5-min | close bucketed into grid at ~17:02–17:04; no clean 17:05 | Fallback only (if `1D` unavailable) |

**Evidence (06/09/2026, raw 1D bars):**
```
US10Y     17:02=4.5200  17:04=4.5160  17:05=4.5200  →[gap]→ 19:30
US5YTIPS  16:56=1.7960               17:05=1.7990  →[gap]→ 19:30
US1YTIPS  17:01=1.0860               17:05=1.0990  →[gap]→ 19:30
US1M      17:01=3.6600               17:05=3.6600  →[gap]→ 19:30
```

### Data provenance (Tradeweb)

CNBC's Treasury/TIPS yield data is sourced from **Tradeweb** (attributed on the CNBC quote pages). Tradeweb, with FTSE Russell, also runs a formal **Tradeweb FTSE U.S. Treasury Benchmark Closing Prices** product (launched 2024-06-10; FTSE Russell = administrator, Tradeweb = calculation agent; IOSCO-compliant). That benchmark snapshots at **3:00pm and 4:00pm ET**; a **2025-10-17 methodology change** emphasizes the **4:00pm ET** "risk-clearing" level. The benchmark is a **paid LSEG/Tradeweb data product** — it is *not* available in the free CNBC chart feed this app uses.

**The values track the bid side.** Compared against broker quotes on the same securities, CNBC's
yields sit closer to the bid than to the ask wherever the two differ. This is an observation from
that comparison, not a documented property of the feed, and the feed is not a single broker's
quotes: treat it as the working assumption when reconciling a CNBC yield against a broker screen.

**Three distinct reference times** (do not conflate):

| Time | What it is | Where it appears |
|---|---|---|
| **3pm / 4pm ET** | Tradeweb **FTSE benchmark closing price** (official, IOSCO; 4pm after 2025-10-17) | CNBC quote-service `previous_day_closing` field (the "~15:00 ET Tradeweb settlement" in *Deviation from CNBC* above) |
| **~17:00 ET** | last *continuous-trading* chart bar | chart intraday stream |
| **17:05 ET** | end-of-session **consolidation bar** at the SIFMA 5:00pm ET cash close (+5min; early-close days 2:00pm ET) | the actual session close; shown in 2D/10D views — **not** the persisted daily baseline |

### Daily-close basis: the ~3 PM benchmark (NOT 17:05)

Empirically, **CNBC's stored daily close equals the ~3:00 PM ET mid-afternoon benchmark, not the 17:05 bar** (verified across US10Y, US10YTIPS, US30YTIPS — daily close matches the 15:00 intraday value within 0–2bp and runs ~1–2bp below 17:05). This aligns with the U.S. Treasury's official **CMT (nominal) and real/TIPS** yield curves, which use **FRBNY indicative quotations at ~3:30 PM ET**, and with the Tradeweb FTSE benchmark (3pm/4pm). The mid-afternoon snapshot is the industry "official close" convention. Full evidence: **[`Close_Price_Investigation.md`](./Close_Price_Investigation.md)**.

**Decision** (feed resolution is daily for 1Y/2Y/3Y, **weekly** at 10Y, **quarterly** at ALL — so only 10Y/ALL need a persistent store):

| Scope | Close basis | Mechanism |
|---|---|---|
| **1Y / 2Y / 3Y** | ~3 PM benchmark | **Reread fresh** from CNBC daily feeds each run — identical to CNBC's window (no drift), no store. |
| **10Y / ALL** | ~3 PM benchmark | **Persistent merge store**: coarse deep history + accumulated **daily** recent closes (finer than CNBC's weekly/quarterly). Persist **completed days only** (run-timing non-critical). |
| **2D / 10D** | actual **17:05** session close (tentative) | already renders real intraday bars incl. the 17:05 bar. |

The 17:05 basis is infeasible for history (the `1D` feed spans only ~2 days). The `IntradayArchive` / `CloseProbe` jobs remain **informational** — they record where the session actually ends; they are not the source for the persisted daily baseline. Full analysis & resolution table: **[`Close_Price_Investigation.md`](./Close_Price_Investigation.md)**.

**Resolved (2026-09-12) — day-change anchor:** the sidebar `closeP` now references the **17:05 session close**, matching the basis 2D/10D charts already use, rather than the earlier `≤17:00` cutoff. See "Yield Change Calculation" above.

---

## Shared Interaction & Navigation
- **Timezone**: All displays are in **Eastern Time (ET)**.
- **Precision**: Yields are displayed with 3 decimal places (e.g., `4.321%`).
- **Panning & Zooming**: 
    - Mouse Wheel: XY zoom.
    - Ctrl+Scroll: X-axis zoom only.
    - Shift+Scroll: Y-axis zoom only.
    - Keyboard (Arrows, +/-): Panning and zooming around view center.
- **Y-Axis Auto-Rescale**: Automatic rescaling to visible data bounds ensures maximum vertical resolution during navigation.
- **Sync Zoom & Pan**: When enabled (default), zooming or panning any time series chart automatically synchronizes the X-axis (time) of all other active charts. This allows for easy comparison of yield movements across different maturities. Y-axis scaling remains independent per chart to maintain optimal resolution for each symbol's yield range.
- **Lock Right**: When enabled, every zoom re-anchors the chart's right edge (X max) to the latest data point, preserving the newly zoomed range — so zooming narrows the window without losing the most recent data (`applyLockRight` in `shared/src/chart-keys.js`).
